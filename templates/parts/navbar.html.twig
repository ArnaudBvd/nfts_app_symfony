<nav class="navbar navbar-expand-lg bg-body-tertiary mb-5 py-3">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path ('app_default')}}">NFTs App</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="#">Tous les NFTs</a>
				</li>

                {% if app.user is not null %}
				<li class="nav-item">
					<a class="nav-link" href="#">Mes NFTs</a>
				</li>
                {% endif %}

                {% if app.user is not null and app.user.hasRoles("ROLE_ADMIN") %}
				<li class="nav-item">
					<a class="nav-link" href="{{path ('app_admin')}}">Administration</a>
				</li>
                {% endif %}

                {% if app.user is null %}
				<li class="nav-item">
					<a class="nav-link" href="{{path ('app_login')}}">Se connecter</a>
				</li>

                <li class="nav-item">
					<a class="nav-link" href="{{path ('app_register')}}">S'enregistrer</a>
				</li>
                {% endif %}
                
                {% if app.user is not null %}
                <li class="nav-item">
					<a class="nav-link" href="{{path ('app_logout')}}">Se déconnecter</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="{{path ('app_nft_form')}}">Ajouter un NFT</a>
				</li>
                {% endif %}
			</ul>
		</div>
	</div>
</nav>
